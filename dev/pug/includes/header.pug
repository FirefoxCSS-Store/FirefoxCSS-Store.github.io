header#main_header
  h3.header-branding FirefoxCSS Store

  nav#main_nav
      a(href='index.html') Theme List
      a#js-themeSwitcher(href='#')
        i.fas.fa-moon
      form.searchform(method='get')
        .inputgroup
          input.searchinput(type='text', name='search', placeholder='Search')
          button.btn-submit.searchsubmit(type='submit')
            i.fas.fa-search
